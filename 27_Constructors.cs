// Los constructores en C# son métodos especiales que se llaman cuando se crea un objeto. Se pueden utilizar para inicializar las propiedades de la clase.

public class Persona
{
    public Persona(string nombre, int edad)
    {
        Nombre = nombre;
        Edad = edad;
    }
}